import React, { Component } from 'react'

export class App extends Component {
    render() {
        const { fetchData, data } = this.props

        return (
            <div></div>
        )
    }
}
